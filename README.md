# Yutoob

A small demo app that looks like YouTube where you can watch videos.

Quick start

1. Install dependencies:

```bash
npm install
```

2. Start the dev server:

```bash
npm run dev
```

Open the URL printed by Vite (usually http://localhost:5173).

Notes

- Videos are sample remote files; feel free to add your own URLs in `src/App.jsx`.
- This is a minimal demo for experimentation.
# The app stores simple per-video stats in `localStorage` and sorts the homepage using a basic recommender that weights total watch time and whether you "like" a video. See `src/utils/recommender.js`.
# 15calut
calut calut calut
